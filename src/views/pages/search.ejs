<!DOCTYPE html>
<html lang="en">
     <head>
          <meta charset="UTF-8" />
          <meta
               name="viewport"
               content="width=device-width, initial-scale=1.0"
          />
          <title>Document</title>
     </head>
     <body>
          <%- include('../partials/header.ejs')%>
          <div class="container">
               <% if(list.length > 0) { %>
               <ul>
                    <% list.forEach(function(user, index) { %>
                    <li><%= index+1 %>. <%= user.username %></li>
                    <% }); %>
               </ul>
               <% } else { %>
               <p>khong co nguoi dung can tim</p>
               <% } %>
          </div>
     </body>
</html>
